function out=get(h,varargin)




    if nargin>1

        out=eval(['h.',varargin{1}]);
    else
        if~isempty(h.ValueDimensions)

            out=struct('Name',{h.Name},...
            'BlockPath',{h.BlockPath},...
            'PortIndex',{h.PortIndex},...
            'SignalName',{h.signalname},...
            'ParentName',{h.parentname},...
            'TimeInfo',{h.TimeInfo},...
            'Time',{h.Time},...
            'Data',{h.Data},...
            'ValueDimensions',{h.ValueDimensions});
        else
            out=struct('Name',{h.Name},...
            'BlockPath',{h.BlockPath},...
            'PortIndex',{h.PortIndex},...
            'SignalName',{h.signalname},...
            'ParentName',{h.parentname},...
            'TimeInfo',{h.TimeInfo},...
            'Time',{h.Time},...
            'Data',{h.Data});
        end
    end
