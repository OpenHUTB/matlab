classdef VSpacer<metric.dashboard.widgets.Widget
    properties(Constant,Hidden)
        HeightLimit=1;
        TooltipLocations=cell.empty(1,0);
    end

    methods
        function verify(~)
        end
    end

end

